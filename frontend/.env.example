# Copy to .env.local for local development. All values are public (inlined at build time).
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws/live
# 1 = run fully offline against synthetic data (no backend needed)
NEXT_PUBLIC_MOCK=0
