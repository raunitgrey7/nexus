-- Tables are created by the backend on startup (SQLAlchemy metadata). This file only makes sure the
-- database exists with useful extensions for JSON querying of event payloads.
CREATE EXTENSION IF NOT EXISTS pgcrypto;
